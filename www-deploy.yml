apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: www
  name: www
spec:
  replicas: 1
  selector:
    matchLabels:
      app: www
  template:
    metadata:
      labels:
        app: www
    spec:
      containers:
      - image: nginx:1.26
        name: nginx
        volumeMounts:
          - name: data
            mountPath: /usr/share/nginx/html
            subPath: site
          - name: data
            mountPath: /etc/nginx/conf.d
            subPath: nginx-conf
      - image: php:8.2-fpm
        name: php
        volumeMounts:
          - name: data
            mountPath: /var/www/html
            subPath: site

      volumes:
        - name: data
          hostPath:
            path: /data
            type: Directory
